describe('context class', () => {
  it('should receives a command and then execute it', () => {
    assert.ok(false);
  });

  it('should resets all operations', () => {
    assert.ok(false);
  });
});